{
  "name": "team-memory-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for team memory storage using SQLite",
  "type": "module",
  "main": "dist/memory-server.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/memory-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
