version: "2"
sql:
  - engine: "sqlite"
    queries: "schema/**/queries.sql"
    schema: "schema/**/schema.sql"
    gen:
      go:
        package: "mdb"
        emit_json_tags: true
        out: "../db-sqlite"
        emit_exact_table_names: true
