-- no_tests_plugin: fixture with no test/ directory.

plugin_info = {
    name        = "no_tests_plugin",
    version     = "1.0.0",
    description = "Plugin without a test directory",
}

function on_init()
end
