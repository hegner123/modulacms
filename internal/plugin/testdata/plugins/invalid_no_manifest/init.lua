-- invalid_no_manifest: test fixture for manager discovery tests.
-- Missing the required plugin_info global. Manager should reject with a
-- logged error during manifest extraction.

local x = 1 + 1
